# This file is an example. Copy it to .env and fill in the values for local development.
# DO NOT COMMIT THE .env FILE.

# You can generate a new secret key with: openssl rand -hex 32
SECRET_KEY=

# You can generate a new Fernet key with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

# The default DATABASE_URL is set in config.py. You can override it here if needed.
# For example, to use a different database file:
# DATABASE_URL=sqlite:///db/another_database.db

# The dev environment defaults to production. You can set it to development for local testing.
# ENV=development
